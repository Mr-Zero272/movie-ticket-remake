export class MovieStatistical {
  public month: number;
  public totalMovies: number;

  constructor(month: number, totalMovies: number) {
    this.month = month;
    this.totalMovies = totalMovies;
  }
}
